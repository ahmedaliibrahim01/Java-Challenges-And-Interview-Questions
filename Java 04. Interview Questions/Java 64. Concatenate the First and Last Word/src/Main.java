// Replace ___ with your code
import java.util.Scanner;

class Main {
    public static String concatFirstAndLastWord(String str) {
        // concatenate the first and the last word of the string
        ___
    }

    public static void main(String[] args) {

        // take input from user
        Scanner scanner = new Scanner(System.in);
        String text = scanner.nextLine();

        // test the withoutString() function with user input
        System.out.println(concatFirstAndLastWord(text));
    }
}